{
  "scripts": {
    "esbuild:build:watch": "node ./esbuild.js --watch",
    "esbuild:dev:watch": "node --watch ./dist/server.js",
    "build": "node esbuild.js",
    "dev": "concurrently \"npm run esbuild:build:watch\" \"npm run esbuild:dev:watch\""
  },
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.18",
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.17",
    "esbuild": "^0.27.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}